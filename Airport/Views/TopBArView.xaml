<UserControl x:Class="Airport.Views.TopBArView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             DataContext="{Binding Source={StaticResource Locator}, Path=TopBarView}"
             Background= "LightCyan">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Asher`s Airport" Style="{StaticResource topBarStyle}"></TextBlock>
        <TextBlock x:Name="clockTB" Text="{Binding Time}" Grid.Column="1" Style="{StaticResource topBarStyle}"/>
    </Grid>
</UserControl>
